Increase the header logo icon size in `src/components/Logo.tsx` by bumping the `flagSize` values:

- sm: `h-8 w-8` → `h-10 w-10`
- md (default, used in SiteHeader): `h-10 w-10` → `h-14 w-14`
- lg: `h-12 w-12` → `h-16 w-16`

This enlarges the crowned Union Jack icon shown next to "UK TEST HUB" in the homepage header without affecting text size or layout spacing.